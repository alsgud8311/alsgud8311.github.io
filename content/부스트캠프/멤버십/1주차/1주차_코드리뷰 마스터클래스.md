### 문제를 두번 해결하자
1. 일단 요구사항을 토대로 다 만들기
2. 리팩토링
3. 리팩토링을 하는 과정에서 필요한 공부하기
1을 하면서 문제를 많이 느낄 것 같음 -> 문제를 개선하면서 느끼는게 많음 -> 글로 남길 수 있는 껀덕지

코드의 일관성 고려하기(함수명, 안의 로직)

- prefix 붙이기
렌더링을 온전히 담당하는 코드
데이터 업데이트 코드

범용적으로 쓰일 수 있는 함수 -> util
종속적인 함수는 util의 성격과는 맞지 않음


코드의 재활용
- 컴포넌트에 대한 템플릿을 만들어서 관리
	- fe에서도 불러오고, be에서도불러오기 

컴포넌트의 이름은 ui가 아닌 보여지는 형태로 만들어야 함